@model Izabella.Models.DeathReason

@{
    ViewData["Title"] = "Details";
}

<h1>Részletek</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-primary">Szerkesztés</a> |
    <a asp-action="Index" class="btn btn-secondary">Vissza a Halálozási okok listájához</a>
</div>